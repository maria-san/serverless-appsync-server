dev:
  STAGE: dev
  REGION: ${env:DEV_AWS_REGION}
  APP_KEY: ${env:DEV_APP_KEY}
  DEBUG: true
  MS_CLIENT_ADAPTER: lambda
  ACCT_ID: ${env:DEV_ACCT_ID}

stg:
  STAGE: stg
  REGION: ${env:STG_AWS_REGION}
  APP_KEY: ${env:STG_APP_KEY}
  DEBUG: false
  MS_CLIENT_ADAPTER: lambda
  ACCT_ID: ${env:STG_ACCT_ID}

prd:
  STAGE: prd
  REGION: ${env:PRD_AWS_REGION}
  APP_KEY: ${env:PRD_APP_KEY}
  DEBUG: false
  MS_CLIENT_ADAPTER: lambda
  ACCT_ID: ${env:PRD_ACCT_ID}
